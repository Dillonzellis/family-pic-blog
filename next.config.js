/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "https://family-pic-blog-production.up.railway.app/",
    ],
  },
};

module.exports = nextConfig;
